# Endpoint for the "Request Early Access" modal form (e.g. a Formspree URL:
# https://formspree.io/f/xxxxxxxx). Must be prefixed PUBLIC_ to be available
# in the browser. When unset, the modal runs in demo mode (logs to console).
PUBLIC_FORM_ENDPOINT=
